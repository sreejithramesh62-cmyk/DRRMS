<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    
    # Allow access to files
    <FilesMatch "\.(html|php|jpg|png|css|js)$">
        Require all granted
    </FilesMatch>
</IfModule>

# Enable directory listing temporarily for debugging
Options +Indexes

# Set default document
DirectoryIndex index.html admin.html login.html
